// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDbY6Q275o7SeN_bjRWb2bh93E21WbF2Tw',
    authDomain: 'shopping-app-a7a07.firebaseapp.com',
    databaseURL: 'https://shopping-app-a7a07.firebaseio.com',
    projectId: 'shopping-app-a7a07',
    storageBucket: 'shopping-app-a7a07.appspot.com',
    messagingSenderId: '869037741314'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
